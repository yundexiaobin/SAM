<% const {config} = data; -%>
import { reactive } from "vue";
import type { FormRules } from "element-plus";

/** 自定义表单规则校验 */
export const formRules = reactive(<FormRules>{
 <% let index = 0; %>
 <% config.formItems.forEach(t=>{ -%>
 <%if(t.required){ -%>
  <%= t.name %>: [{ required: <%= t.required %>, message: "<%= t.description %>为必填项", trigger: "blur" }]<% index=index+1;-%><% if(index<config.formItems.length){ %>,<% } %>
 <% } -%>
<% }) -%>
});
